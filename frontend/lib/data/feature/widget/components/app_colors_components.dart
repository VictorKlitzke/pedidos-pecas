import 'package:flutter/material.dart';

class AppColorsComponents {
  // Cores primárias
  static const primary = Color(0xFF1A73E8); // Azul vibrante (cor principal)
  static const primaryDark = Color(0xFF1657B3); // Azul mais escuro
  static const primaryLight = Color(0xFF63A4FF); // Azul mais claro

  // Cores secundárias
  static const secondary = Color(0xFFFFC107); // Amarelo vibrante (cor de destaque)
  static const secondaryDark = Color(0xFFC79100); // Amarelo mais escuro
  static const secondaryLight = Color(0xFFFFF350); // Amarelo mais claro

  // Cores neutras
  static const background = Color(0xFFF5F5F5); // Fundo claro
  static const surface = Color(0xFFFFFFFF); // Superfície branca
  static const onBackground = Color(0xFF333333); // Texto escuro sobre fundo claro
  static const onSurface = Color(0xFF000000); // Texto preto sobre superfície branca

  // Cores de feedback
  static const success = Color(0xFF4CAF50); // Verde para sucesso
  static const error = Color(0xFFE53935); // Vermelho para erro
  static const warning = Color(0xFFFFA000); // Laranja para aviso
  static const info = Color(0xFF2196F3); // Azul para informações

  // Cores adicionais
  static const accent = Color(0xFF9C27B0); // Roxo para destaques
  static const accentDark = Color(0xFF6A1B9A); // Roxo mais escuro
  static const accentLight = Color(0xFFCE93D8); // Roxo mais claro
}